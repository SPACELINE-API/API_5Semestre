{
  "name": "@api/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:python": "python -m pip install -r requirements.txt",
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 3333",
    "lint": "ruff check .",
    "lint:fix": "ruff check . --fix",
    "format": "ruff format .",
    "format:check": "ruff format --check"
  }
}
