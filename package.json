{
  "name": "api-5semestre",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "pnpm --filter @api/server install:python",
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter @api/web dev",
    "dev:server": "pnpm --filter @api/server dev",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7"
  },
  "type": "module"
}
